{
  "checks": {
    "check_basic_disks": {
      "command": "/etc/sensu/plugins/basic/check-disk.rb -d -w 75 -c 90 -x nfs -i /boot -i /dev/shm",
      "handlers": [
        "mailer",
        "kpi-handler"
      ],
      "subscribers": [
        "basic"
      ],
      "interval": 60
    }
  }
}
